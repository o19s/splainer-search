# esbuild IIFE output (npm run build / prepublishOnly)
dist/
# generic (system) files/extensions we don't want
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz
.idea/*
*.DS_Store
lib-cov
pids
logs
results

node_modules

bower_components

# karma's coverage reporting
coverage

# Stryker mutation testing (instrumented copies and temp output)
.stryker-tmp
reports/mutation

#vim
*.swp
